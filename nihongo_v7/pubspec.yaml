name: nihongo_lens

description: Japanese subtitle OCR translator

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.13.0
  google_mlkit_translation: ^0.11.0

flutter:
  uses-material-design: true
